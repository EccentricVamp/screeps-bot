on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: EccentricVamp/screeps-action@cb8a96e8e070f6fc1cb5f77e6e798cdca79df935
        with:
          token: doot
          # branch: ${{ github.ref }}
