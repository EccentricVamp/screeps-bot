on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: EccentricVamp/screeps-action@3555da44a2159404d9f3636eb98b817a82654b8f
        with:
          token: doot
          # branch: ${{ github.ref }}
